# Direct Python dependencies for Journeyman.
#
# This file is maintainer-facing: it records packages Journeyman depends on
# directly. Production/release installs MUST use requirements.lock (or
# requirements-postgresql.lock) so the complete transitive dependency graph is
# reproducible.
Flask==3.1.3
Flask-SQLAlchemy==3.1.1
Flask-Migrate==4.1.0
Flask-WTF==1.3.0
gunicorn==23.0.0

cryptography==50.0.0
ldap3==2.9.1
packaging==26.2
PyYAML==6.0.3
requests==2.33.0
